# cast_py_client

Cast Python client (`CastClient`, `SlicerCastClient`, event-name helpers).

## Install

From monorepo root:

```bash
pip install -e cast_py_client
```

## Use

```python
from cast_client import SlicerCastClient, HubConfig, SessionConfig
```
